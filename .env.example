# Backend API origin (proxied by Next.js rewrites)
NEXT_PUBLIC_API_ORIGIN=https://XXX.execute-api.ca-central-1.amazonaws.com/dev

# API Gateway key (same as MasTER / SARA desktop)
NEXT_PUBLIC_API_KEY=XXX
